# Runtime data — the actual member list lives here, not in git.
members.db
members.db-wal
members.db-shm

# Generated from members.db — contains real names
seed.sql

# Pre-SQLite data and its backup
members.json
members.json.tmp
members.json.backup

# wrangler's local state and build cache
.wrangler/
node_modules/

.DS_Store
