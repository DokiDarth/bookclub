# Runtime data — the actual member list lives here, not in git.
members.db
members.db-wal
members.db-shm

# Pre-SQLite data and its backup
members.json
members.json.tmp
members.json.backup

.DS_Store
